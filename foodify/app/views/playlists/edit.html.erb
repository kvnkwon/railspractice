<%= form_for @playlist, url: user_playlist_path, :html => { :multipart => true } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: @playlist.name %>
  <%= f.submit "Update", class: "btn btn-success" %>
<% end %>
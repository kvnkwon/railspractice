<%= form_for @item, url: user_playlist_items_path do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: "What is the food's name?" %>

  <!-- Search for restaurant and location using Yelp SEARCH API. This will return 5 results.
  Allow user to select the correct one out of the results that Yelp returns.
  Get the restaurant they chose, look in Restaurants table to see if it exists, if not create it.
  Add that restaurant's ID to this item.
  <%= f.label :term, "Restaurant's name?" %>
  <%= f.text_field :term %>
  <%= f.label :location, "Where is it?" %>
  <%= f.text_field :location %> -->

  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>

